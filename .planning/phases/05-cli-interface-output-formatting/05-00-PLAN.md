---
phase: 05-cli-interface-output-formatting
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - tests/05-cli-interface.test.js
autonomous: true
requirements: [OUT-01, OUT-02, OUT-03, OUT-04, OUT-05, CLI-01, CLI-02, CLI-03, CLI-04, CLI-05, CLI-06, CLI-07]

must_haves:
  truths:
    - "Commander.js 14.0.3 is importable from usage.mjs"
    - "Test file exists with stubs for all 12 Phase 05 requirements"
  artifacts:
    - path: "package.json"
      provides: "ESM module declaration + commander dependency"
      contains: "commander"
    - path: "tests/05-cli-interface.test.js"
      provides: "Test stubs for OUT-01~05, CLI-01~07"
      min_lines: 80
  key_links: []
---

<objective>
Prepare test infrastructure and install Commander.js dependency before implementation begins.

Purpose: Commander.js is not yet installed and no test file exists for Phase 05. This wave 0 task creates the foundation so subsequent plans can implement with TDD support.
Output: package.json with commander dependency, installed node_modules, test file with stubs for all 12 requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cli-interface-output-formatting/05-CONTEXT.md
@.planning/phases/05-cli-interface-output-formatting/05-RESEARCH.md
@usage.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create package.json, install Commander.js, create test stubs</name>
  <files>package.json, tests/05-cli-interface.test.js</files>
  <read_first>
    usage.mjs (lines 1-10 for VERSION/DESCRIPTION constants, lines 1238-1287 for export block and existing test patterns)
    tests/04-caching-layer.test.js (for established test patterns and conftest.js usage)
  </read_first>
  <action>
Per D-11: Commander.js 14.0.3 is the ONLY external dependency for this project. Create the minimal package.json and install it.

**Step 1: Create package.json**
Create `/Users/abrahamgreyson/Code/cc-third-party-usage/package.json` with:
```json
{
  "name": "cc-third-party-usage",
  "version": "1.0.0",
  "type": "module",
  "description": "AI API Usage Monitor - Query Kimi and GLM API usage with CC Switch proxy detection",
  "dependencies": {
    "commander": "14.0.3"
  }
}
```

**Step 2: Install Commander.js**
Run `npm install` in the project root. This creates node_modules/ and package-lock.json.

**Step 3: Create test stubs file**
Create `tests/05-cli-interface.test.js` following the pattern from tests/04-caching-layer.test.js (import from conftest.js, use describe/test.todo blocks).

The test file MUST import `describe, it, test, expect, beforeEach, mock` from 'bun:test' and import all needed functions from usage.mjs (the existing exports plus the new ones that will be added: `formatCompactTime`, `buildPlaceholderValues`, `applyTemplate`, `formatDefaultOutput`, `outputVerboseInfo`, `runCLI`).

Create test.todo() stubs organized into these describe blocks:
1. "formatCompactTime" - 4 stubs: formats hours+minutes (e.g., 2h30m), formats minutes+seconds (e.g., 45m15s), formats days+hours (e.g., 3d12h), handles zero/negative input
2. "buildPlaceholderValues" - 3 stubs: builds flat lookup from nested quotas array, uses window prefix (5h_percent, weekly_reset), handles missing/null values
3. "applyTemplate" - 4 stubs: replaces {provider} placeholder, replaces window-prefixed placeholders ({5h_percent}, {weekly_reset}), keeps unknown placeholders as-is, handles empty template
4. "formatDefaultOutput" - 3 stubs: outputs "Provider: X% | YhZm" format, selects shortest reset window, capitalizes provider name
5. "outputVerboseInfo" - 3 stubs: writes [debug] lines to stderr, outputs cache/provider/API sections, handles missing info gracefully
6. "runCLI" - 4 stubs: parses --json flag, parses --template flag, parses --cache-duration with default 60, handles --version flag
7. "stdout/stderr separation" - 2 stubs: data output goes to stdout, errors+verbose go to stderr

Total: approximately 23 test.todo() stubs.
  </action>
  <verify>
    <automated>cd /Users/abrahamgreyson/Code/cc-third-party-usage && npm ls commander 2>&1 | grep -q "commander@14.0.3" && echo "PASS: commander installed" || echo "FAIL: commander not found"</automated>
  </verify>
  <done>
    - package.json exists with `"type": "module"` and `"commander": "14.0.3"` in dependencies
    - node_modules/commander/ directory exists
    - tests/05-cli-interface.test.js exists with ~23 test.todo() stubs organized into 7 describe blocks
    - `npm ls commander` shows commander@14.0.3
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/abrahamgreyson/Code/cc-third-party-usage
npm ls commander
bun test tests/05-cli-interface.test.js
```
</verification>

<success_criteria>
- package.json with commander dependency created and installed
- Test stub file covers all 12 Phase 05 requirements (OUT-01~05, CLI-01~07)
- Test runner discovers all stubs without errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-cli-interface-output-formatting/05-00-SUMMARY.md`
</output>

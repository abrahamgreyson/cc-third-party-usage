---
phase: 01-core-infrastructure
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - test/runtime-detection.test.js
  - test/error-handling.test.js
  - test/database-bun.test.js
  - test/database-node.test.js
  - test/database-wal.test.js
  - test/database-busy-timeout.test.js
  - test/http-client.test.js
  - test/http-timeout.test.js
  - test/http-retry.test.js
  - test/http-rate-limit.test.js
  - test/http-error-messages.test.js
  - tests/conftest.js
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Developer can run `bun test` and see all test stubs execute"
    - "Each test file has describe blocks for its mapped requirements"
  artifacts:
    - path: "test/runtime-detection.test.js"
      provides: "Test stubs for CORE-01, CORE-02, DB-03"
      contains: "describe"
    - path: "test/error-handling.test.js"
      provides: "Test stubs for CORE-03, CORE-04"
      contains: "describe"
    - path: "tests/conftest.js"
      provides: "Shared test fixtures"
      contains: "export"
  key_links:
    - from: "test/*.test.js"
      to: "tests/conftest.js"
      via: "import"
      pattern: "from.*conftest"
---

<objective>
Create test infrastructure stubs for Phase 1 requirements validation.

Purpose: Enable TDD workflow with test stubs that fail initially, providing verification scaffolding for subsequent plans.
Output: 11 test files with describe blocks + shared fixture module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-infrastructure/01-RESEARCH.md
@.planning/phases/01-core-infrastructure/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared test fixtures module</name>
  <files>tests/conftest.js</files>
  <read_first>
    - .planning/phases/01-core-infrastructure/01-RESEARCH.md (lines 562-608 for test structure guidance)
  </read_first>
  <action>Create `tests/conftest.js` with the following exports:

1. `createTestDatabase(dbPath)` - Creates temporary SQLite database for testing, returns database instance and cleanup function
2. `mockFetchResponse(data, options)` - Creates mock Response object for fetch testing with status, headers, and JSON body
3. `withTimeout(promise, ms)` - Helper to assert promises resolve within time limit
4. `sleep(ms)` - Promise-based delay for async testing

Implementation details:
- Use Bun's built-in test utilities if available, otherwise pure JS implementations
- createTestDatabase should create a temp file in /tmp with random suffix
- mockFetchResponse should return object with ok, status, statusText, headers.get(), json() method
- All functions should be small, focused, and pure where possible

Code structure:
```javascript
///// Test Fixtures /////

import { mkdtempSync, writeFileSync, rmSync } from 'fs';
import { tmpdir } from 'os';
import { join } from 'path';

export function createTestDatabase(dbPath) {
  // Create temp directory and return db path + cleanup function
}

export function mockFetchResponse(data, options = {}) {
  // Return mock Response object
}

export function withTimeout(promise, ms) {
  // Race promise against timeout
}

export function sleep(ms) {
  // Return promise that resolves after ms
}
```
</action>
  <verify>
    <automated>test -f tests/conftest.js && grep -q "export function createTestDatabase" tests/conftest.js && grep -q "export function mockFetchResponse" tests/conftest.js</automated>
  </verify>
  <acceptance_criteria>
    - `tests/conftest.js` exists and is valid JavaScript
    - File contains `export function createTestDatabase`
    - File contains `export function mockFetchResponse`
    - File contains `export function withTimeout`
    - File contains `export function sleep`
  </acceptance_criteria>
  <done>Shared test fixtures module created with all four helper functions exported.</done>
</task>

<task type="auto">
  <name>Task 2: Create runtime detection test stubs</name>
  <files>test/runtime-detection.test.js</files>
  <read_first>
    - tests/conftest.js (created in Task 1)
    - .planning/phases/01-core-infrastructure/01-RESEARCH.md (lines 109-157 for runtime detection patterns)
  </read_first>
  <action>Create `test/runtime-detection.test.js` with test stubs for:

1. **CORE-01: Single-file ESM architecture**
   - Test: "should export usage.mjs as ESM module"
   - Stub: `it.todo("should export usage.mjs as ESM module with zero external dependencies")`

2. **CORE-02: Cross-runtime compatibility**
   - Test: "should detect Bun runtime correctly"
   - Test: "should detect Node.js runtime correctly"
   - Test: "should throw error for unsupported runtime"
   - Stubs with `it.todo()`

3. **DB-03: Runtime detection and conditional import**
   - Test: "should import bun:sqlite when running under Bun"
   - Test: "should import node:sqlite when running under Node.js"
   - Test: "should throw clear error for unsupported runtime"

Structure:
```javascript
import { describe, it, expect } from 'bun:test';

///// CORE-01: Single-file ESM Architecture /////

describe('CORE-01: Single-file ESM Architecture', () => {
  it.todo('should export usage.mjs as ESM module with zero external dependencies');
});

///// CORE-02: Cross-runtime Compatibility /////

describe('CORE-02: Cross-runtime Compatibility', () => {
  it.todo('should detect Bun runtime correctly using process.versions.bun');
  it.todo('should detect Node.js runtime correctly');
  it.todo('should throw error for unsupported runtime with clear message');
});

///// DB-03: Runtime Detection and Conditional Import /////

describe('DB-03: Runtime Detection and Conditional Import', () => {
  it.todo('should import bun:sqlite when running under Bun');
  it.todo('should import node:sqlite when running under Node.js');
  it.todo('should throw clear error when SQLite module unavailable');
});
```
</action>
  <verify>
    <automated>bun test test/runtime-detection.test.js 2>&1 | grep -q "CORE-01"</automated>
  </verify>
  <acceptance_criteria>
    - `test/runtime-detection.test.js` exists
    - File contains `describe('CORE-01: Single-file ESM Architecture'`
    - File contains `describe('CORE-02: Cross-runtime Compatibility'`
    - File contains `describe('DB-03: Runtime Detection and Conditional Import'`
    - File has at least 7 `it.todo(` calls total
  </acceptance_criteria>
  <done>Runtime detection test stubs created with describe blocks for CORE-01, CORE-02, and DB-03.</done>
</task>

<task type="auto">
  <name>Task 3: Create error handling test stubs</name>
  <files>test/error-handling.test.js</files>
  <read_first>
    - tests/conftest.js (created in Task 1)
    - .planning/phases/01-core-infrastructure/01-RESEARCH.md (lines 263-324 for error handling patterns)
    - .planning/phases/01-core-infrastructure/01-discussion-log.md (lines 39-57 for user decisions on error handling)
  </read_first>
  <action>Create `test/error-handling.test.js` with test stubs for:

1. **CORE-03: Fail-fast error handling**
   - Test: "should throw UsageError with actionable message"
   - Test: "should throw ConfigError with actionable message"
   - Test: "should throw APIError with actionable message"
   - Test: "should throw NetworkError with actionable message"

2. **CORE-04: Semantic exit codes**
   - Test: "should exit with code 0 on success"
   - Test: "should exit with code 1 on general error"
   - Test: "should exit with code 2 on config error"
   - Test: "should exit with code 3 on network error"
   - Test: "should exit with code 4 on API error"

User decision (from discussion log):
- Custom Error classes: UsageError, ConfigError, APIError, NetworkError
- Exit codes: 0=success, 1=error, 2=config error, 3=network error, 4=API error
- Error messages: Concise + actionable suggestions

Structure:
```javascript
import { describe, it, expect } from 'bun:test';

///// CORE-03: Fail-fast Error Handling /////

describe('CORE-03: Fail-fast Error Handling', () => {
  it.todo('should throw UsageError with actionable message');
  it.todo('should throw ConfigError with actionable message');
  it.todo('should throw APIError with actionable message');
  it.todo('should throw NetworkError with actionable message');
});

///// CORE-04: Semantic Exit Codes /////

describe('CORE-04: Semantic Exit Codes', () => {
  it.todo('should exit with code 0 on success');
  it.todo('should exit with code 1 on general error');
  it.todo('should exit with code 2 on config error');
  it.todo('should exit with code 3 on network error');
  it.todo('should exit with code 4 on API error');
});
```
</action>
  <verify>
    <automated>bun test test/error-handling.test.js 2>&1 | grep -q "CORE-03"</automated>
  </verify>
  <acceptance_criteria>
    - `test/error-handling.test.js` exists
    - File contains `describe('CORE-03: Fail-fast Error Handling'`
    - File contains `describe('CORE-04: Semantic Exit Codes'`
    - File has at least 9 `it.todo(` calls total
  </acceptance_criteria>
  <done>Error handling test stubs created with describe blocks for CORE-03 and CORE-04.</done>
</task>

<task type="auto">
  <name>Task 4: Create database test stubs</name>
  <files>test/database-bun.test.js, test/database-node.test.js, test/database-wal.test.js, test/database-busy-timeout.test.js</files>
  <read_first>
    - tests/conftest.js (created in Task 1)
    - .planning/phases/01-core-infrastructure/01-RESEARCH.md (lines 109-157 for SQLite patterns, lines 347-357 for pitfalls)
  </read_first>
  <action>Create 4 database test files with stubs:

**test/database-bun.test.js (DB-01):**
```javascript
import { describe, it, expect } from 'bun:test';

///// DB-01: bun:sqlite for Bun Runtime /////

describe('DB-01: bun:sqlite for Bun Runtime', () => {
  it.todo('should import bun:sqlite when running under Bun');
  it.todo('should open database using bun:sqlite Database class');
  it.todo('should execute prepared statements with bun:sqlite');
});
```

**test/database-node.test.js (DB-02):**
```javascript
import { describe, it, expect } from 'bun:test';

///// DB-02: node:sqlite for Node.js Runtime /////

describe('DB-02: node:sqlite for Node.js Runtime', () => {
  it.todo('should import node:sqlite when running under Node.js');
  it.todo('should open database using node:sqlite DatabaseSync class');
  it.todo('should execute prepared statements with node:sqlite');
});
```

**test/database-wal.test.js (DB-04):**
```javascript
import { describe, it, expect } from 'bun:test';

///// DB-04: WAL Mode for Concurrent Access /////

describe('DB-04: WAL Mode for Concurrent Access', () => {
  it.todo('should enable WAL mode on database open');
  it.todo('should allow concurrent reads with WAL enabled');
  it.todo('should verify journal_mode is WAL after open');
});
```

**test/database-busy-timeout.test.js (DB-05):**
```javascript
import { describe, it, expect } from 'bun:test';

///// DB-05: Busy Timeout for Locking Prevention /////

describe('DB-05: Busy Timeout for Locking Prevention', () => {
  it.todo('should set busy_timeout to 10000ms on database open');
  it.todo('should wait up to 10 seconds before throwing SQLITE_BUSY');
  it.todo('should verify busy_timeout pragma is applied');
});
```
</action>
  <verify>
    <automated>test -f test/database-bun.test.js && test -f test/database-node.test.js && test -f test/database-wal.test.js && test -f test/database-busy-timeout.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `test/database-bun.test.js` exists with DB-01 describe block
    - `test/database-node.test.js` exists with DB-02 describe block
    - `test/database-wal.test.js` exists with DB-04 describe block
    - `test/database-busy-timeout.test.js` exists with DB-05 describe block
    - Each file has at least 3 `it.todo(` calls
  </acceptance_criteria>
  <done>Database test stubs created for DB-01, DB-02, DB-04, and DB-05.</done>
</task>

<task type="auto">
  <name>Task 5: Create HTTP client test stubs</name>
  <files>test/http-client.test.js, test/http-timeout.test.js, test/http-retry.test.js, test/http-rate-limit.test.js, test/http-error-messages.test.js</files>
  <read_first>
    - tests/conftest.js (created in Task 1)
    - .planning/phases/01-core-infrastructure/01-RESEARCH.md (lines 162-260 for HTTP patterns)
    - .planning/phases/01-core-infrastructure/01-discussion-log.md (lines 22-37 for user decisions on retry strategy)
  </read_first>
  <action>Create 5 HTTP test files with stubs:

**test/http-client.test.js (HTTP-01):**
```javascript
import { describe, it, expect } from 'bun:test';

///// HTTP-01: Native Fetch API /////

describe('HTTP-01: Native Fetch API', () => {
  it.todo('should use native fetch without external dependencies');
  it.todo('should make GET request with Authorization header');
  it.todo('should return Response object with json() method');
});
```

**test/http-timeout.test.js (HTTP-02):**
```javascript
import { describe, it, expect } from 'bun:test';

///// HTTP-02: Request Timeout (5s default) /////

describe('HTTP-02: Request Timeout (5s default)', () => {
  it.todo('should timeout after 5 seconds by default');
  it.todo('should accept custom timeout via options');
  it.todo('should throw AbortError on timeout with clear message');
  it.todo('should cleanup timeout on successful response');
});
```

**test/http-retry.test.js (HTTP-03):**
User decision: Exponential backoff (1s, 2s, 4s), max 3 retries, 4xx client errors fail immediately

```javascript
import { describe, it, expect } from 'bun:test';

///// HTTP-03: Retry with Exponential Backoff /////

describe('HTTP-03: Retry with Exponential Backoff', () => {
  it.todo('should retry up to 3 times with exponential backoff (1s, 2s, 4s)');
  it.todo('should not retry on 4xx client errors (400, 401, 403, 404)');
  it.todo('should retry on 5xx server errors (500, 502, 503, 504)');
  it.todo('should retry on network errors (ETIMEDOUT, ECONNRESET, ECONNREFUSED)');
  it.todo('should retry on 429 rate limit with exponential backoff');
  it.todo('should throw last error after max retries exhausted');
});
```

**test/http-rate-limit.test.js (HTTP-04):**
```javascript
import { describe, it, expect } from 'bun:test';

///// HTTP-04: Rate Limit (429) Handling /////

describe('HTTP-04: Rate Limit (429) Handling', () => {
  it.todo('should respect Retry-After header on 429 response');
  it.todo('should use exponential backoff if Retry-After not present');
  it.todo('should cap retry delay at 10 seconds max');
});
```

**test/http-error-messages.test.js (HTTP-05):**
```javascript
import { describe, it, expect } from 'bun:test';

///// HTTP-05: Clear Error Messages /////

describe('HTTP-05: Clear Error Messages', () => {
  it.todo('should provide actionable message on network failure');
  it.todo('should provide actionable message on timeout');
  it.todo('should provide actionable message on 4xx client error');
  it.todo('should provide actionable message on 5xx server error');
  it.todo('should include request URL and method in error message');
});
```
</action>
  <verify>
    <automated>ls test/http-*.test.js | wc -l | grep -q "5"</automated>
  </verify>
  <acceptance_criteria>
    - `test/http-client.test.js` exists with HTTP-01 describe block
    - `test/http-timeout.test.js` exists with HTTP-02 describe block
    - `test/http-retry.test.js` exists with HTTP-03 describe block
    - `test/http-rate-limit.test.js` exists with HTTP-04 describe block
    - `test/http-error-messages.test.js` exists with HTTP-05 describe block
    - Each file has at least 3 `it.todo(` calls
  </acceptance_criteria>
  <done>HTTP client test stubs created for HTTP-01 through HTTP-05.</done>
</task>

<task type="auto">
  <name>Task 6: Verify test infrastructure</name>
  <files>None (verification only)</files>
  <read_first>
    - All test files created in Tasks 1-5
  </read_first>
  <action>Run `bun test` to verify all test stubs are valid and can be executed. All tests should show as "todo" or skipped, not failing with syntax errors.

Expected output should show:
- 11 test files discovered
- All describe blocks rendered
- All it.todo items listed

If any test file has syntax errors, fix them.
</action>
  <verify>
    <automated>bun test 2>&1 | grep -c "todo" | head -1</automated>
  </verify>
  <acceptance_criteria>
    - `bun test` command exits without syntax errors
    - Output shows test files discovered (at least 11)
    - No "SyntaxError" or "Cannot find module" errors in output
  </acceptance_criteria>
  <done>All test stubs verified working with `bun test` command.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run `bun test` - should show all test stubs without syntax errors
2. Verify 11 test files exist in test/ directory
3. Verify tests/conftest.js exists with 4 exported functions
</verification>

<success_criteria>
- 11 test files created with describe blocks matching VALIDATION.md mapping
- tests/conftest.js created with shared fixtures
- `bun test` runs without syntax errors
- All test stubs use `it.todo()` format for TDD workflow
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-infrastructure/01-00-SUMMARY.md`
</output>
